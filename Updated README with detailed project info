## Java-OOPS-Multilevel-Inheritance

This Java project demonstrates **multilevel inheritance** using three classes:
- `Animal` (base class)
- `Dog` (inherits from `Animal`)
- `Puppy` (inherits from `Dog`)

### Features:
- Method overriding
- Class-specific methods (`bark`, `weep`, `details`)
- Demonstration through the `Test` class

### How to Run:
1. Compile all `.java` files.
2. Run `Test.java`.
